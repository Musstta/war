# Persistent World Strategy Game — Tech Stack & Build Plan

**Companion to:** `persistent-world-design.md` (the game design doc — the source of truth for *what* to build). This document covers *how* to build it and in *what order*. Hand both documents to Claude Code together.

**Operator profile:** Solo developer, comfortable coding, Docker, and databases; wants Claude Code to manage day-to-day implementation while retaining a supervisor role and the ability to intervene manually. Priority: a **boring, single-language, well-documented stack** with nothing clever to relearn when something breaks at 11pm.

**Deployment target:** Self-hosted on a home server (Lenovo M170Q), exposed via a Cloudflare Tunnel at a subdomain such as `war.musstta.cc`. The app is a long-running web service — it must run 24/7 and never spin down (the midnight tick depends on it).

**Players:** ~5 to start. One responsive website, usable well on **both mobile and desktop**. No native apps. Sessions are short (~10–15 min/day).

---

## 1. Guiding Principles

1. **This is a database + a tick function, not a graphics problem.** Do NOT use a game engine (Unity, Godot, etc.). The right tools are a normal backend language, a normal database, and a normal web frontend.
2. **One language end to end** where possible — reduces context-switching for a solo dev. **TypeScript** is the recommendation: it runs the backend, the frontend, and the headless simulation, all in one language with one toolchain.
3. **Boring and well-documented beats clever.** Every library chosen below is mainstream with large communities — troubleshooting answers exist.
4. **The simulation engine is the product.** Build it first, build it headless, build it testable. See Section 6 and the build order in Section 8.
5. **Territories are data, not code.** The engine loads the world (territories, attributes, adjacency) from a data file at startup. The game works the same with 40 territories or 500. See Section 5.
6. **Architect for the deferred features now.** The design doc defers route interdiction and several other features but mandates that the data model support them. Respect those notes — they are cheap now and expensive later.

---

## 2. Recommended Stack

| Layer | Recommendation | Why |
|---|---|---|
| Language | **TypeScript** everywhere | One language for backend, frontend, and simulation. Strong typing matters a lot for a rules-heavy game with complex state. |
| Runtime | **Node.js** (LTS) | Boring, universal, runs fine on the M170Q. |
| Backend framework | **Fastify** (or Express if preferred) | Lightweight HTTP API. Fastify is fast and well-documented; Express is even more universally documented if that matters more than speed. |
| Database | **PostgreSQL** | The world state is relational (nations, territories, treaties, routes, armies). Postgres is rock-solid, transactional (critical for atomic tick resolution), and self-hosts trivially in Docker. |
| DB access | **Prisma** (ORM) | Type-safe queries, schema migrations, readable. Good fit for a solo dev who wants Claude Code to manage schema changes safely. |
| Frontend framework | **React** | Largest ecosystem, best map-library support, Claude Code is strongest here. |
| Frontend build/tooling | **Vite** | Fast, simple, the current default. |
| Styling | **Tailwind CSS** | Fast to iterate, responsive utilities make mobile+desktop straightforward. |
| Map rendering | **MapLibre GL JS** (see Section 4) | Open-source, no API keys, renders real geographic vector data, pans/zooms, mobile-friendly. |
| Scheduled tick | **node-cron** inside the backend process, OR a system cron calling an endpoint | Runs the midnight tick. See Section 6. |
| Auth | **Lucia** or a simple session-cookie scheme | 5 known players — auth can be minimal. Do not over-build this. |
| Containerization | **Docker + Docker Compose** | One `docker-compose.yml` runs the app + Postgres. Matches the operator's existing workflow. |
| Process management | Docker's restart policy (`restart: unless-stopped`) | Ensures the service comes back after a reboot — essential for a 24/7 tick. |

**Repo shape:** a single monorepo with three packages — `engine/` (pure simulation, no HTTP, no DB-specific code), `server/` (HTTP API + DB + tick scheduler, depends on `engine/`), `web/` (React frontend). The engine being a standalone package is what makes headless simulation possible (Section 6).

---

## 3. Why NOT certain tempting choices

- **No game engine.** Unity/Godot solve rendering and physics. This game has neither. They would add enormous complexity for zero benefit.
- **No NoSQL / document DB as the primary store.** The world is highly relational and the tick must be transactional. Postgres is the correct call. (A document store could *supplement* later, e.g. for the Event Log, but is not needed.)
- **No serverless / edge functions.** A persistent ticking world needs a long-running process. Serverless spins down. The home server is the right model.
- **No 3D globe.** The design calls for a real geographic map, not a globe. A 2D vector map (MapLibre) is the correct, far cheaper choice.

---

## 4. The Map — Approach

The design calls for a real-world geographic map (real coastlines and borders) with **modest** detail — it should *read* as Earth; perfect fidelity (gulfs, tiny inlets) is explicitly not required.

**Rendering:** **MapLibre GL JS** — open-source, no API key, no usage limits, renders vector polygons, supports pan/zoom and click-to-select, works on mobile. Territories are rendered as colored polygons (fill = owner nation); selection and fog-of-war are styling layers on top.

**Boundary data:** **Natural Earth** (naturalearthdata.com) — public domain, free, the standard source for country and state/province boundaries at multiple detail levels. Use a **low or medium detail tier** — it matches the "looks like Earth, doesn't need gulfs" requirement and renders fast on mobile. Admin-0 (countries) and Admin-1 (states/provinces) layers together cover the territory model in Section 5.

**Polygon simplification:** raw boundary data is high-resolution. Simplify the polygons (e.g. with `mapshaper`, a free tool) so the map loads fast — especially important on mobile. This is a one-time content step.

**What you build vs. get for free:** map *rendering*, *pan/zoom*, and *boundary data* are free/prebuilt. The *territory definitions*, *adjacency graph*, and all *game logic* are yours to build — correctly, because that is the game.

---

## 5. The Territory Model — Build as Data, Not Code

The design wants real countries and regions, with a curation rule: **subdivide the giants** (US, Canada, Russia, China → administrative regions), **keep mid-size countries whole** (e.g. Costa Rica = one territory), **absorb micro-states** into a regional territory (e.g. Liechtenstein folds into a surrounding European territory). This is standard, sound game-map curation — not too complex to reason about. The only genuinely tedious part is the **adjacency data**: for every territory, which territories border it, and whether it is coastal.

**Principle: the world is a data file the engine loads at startup.** The engine must not hardcode territory count or identities. It reads a structured file (JSON) describing every territory: id, name, attributes (Population, Industry, Wealth, Geography, Culture per the design doc), the list of adjacent territory ids, and coastal flag. The map frontend reads the same ids to color the right polygons.

**Consequence — the build order this unlocks:** the first playable build uses a **deliberately small, hand-curated territory set** (~40–60 territories, the curation rules applied to one or two continents). The full 200–500 real-world world is built **later, as content**, by editing the data file — *with no engine changes*. This is not "simple territories vs. real world"; it is building the real-world map in the correct order: **after the engine works.**

**[OPEN] territory questions** (carry into the design doc's open list):
- Which continent(s) for the initial ~40–60 territory set.
- The exact micro-state absorption mapping.
- How adjacency is authored — by hand, or semi-derived from polygon borders then hand-corrected.

---

## 6. The Simulation Engine — The Heart of the Project

This is the single most important architectural point. From the design doc Section 17: the game is a server-state and concurrency project, and the engine must be **headless and fast-forwardable**.

**`engine/` is a pure, standalone TypeScript package.** It has no HTTP code and no direct database calls. Its core is one function, conceptually:

> `resolveTick(worldState, queuedActions) → newWorldState`

Given a world state and the set of actions queued for that tick, it deterministically produces exactly one next world state. Pure in, pure out.

**Why this shape matters:**
- **Headless simulation:** a separate script can loop `resolveTick` thousands of times over made-up actions to fast-forward 100+ game-days in seconds. This is the *only* way to tune the open numbers (Mandate formulas, friction curves, unrest rates, war exhaustion, starting-position balance). Tuning by playing the real game is impossibly slow.
- **Testability:** pure functions are trivially unit-testable. Given state X and actions Y, assert the result.
- **Determinism + seeded randomness:** all randomness (combat rolls, trait drift) draws from a seeded RNG passed into the tick. A tick can be replayed exactly for debugging.
- **Crash safety:** because the tick is atomic, the `server/` package can wrap each real tick in a single database transaction — if the process dies mid-tick, the DB rolls back to a clean prior state.

**`server/` consumes the engine:** it loads world state from Postgres, collects the actions players queued during the day, calls `resolveTick`, and writes the new state back inside one transaction. Player actions during the Main/Preparation phases are stored as **queued intents** (rows in the DB), never applied live — this is what makes two players acting on the same territory safe (resolved together at the tick) and what the design doc Section 17 requires.

**The tick scheduler:** at the configured server time (midnight, Costa Rica / UTC-6), trigger one tick. Either `node-cron` inside the backend process, or a system cron hitting a protected endpoint. Keep it simple; make sure the timezone is fixed and explicit in config.

**Build a simulation harness early.** A CLI script in the repo that spins up a fake world, runs N ticks with scripted or random actions, and prints/plots the resulting numbers. This harness is how every `[OPEN]` number in the design doc gets answered.

---

## 7. Other Architecture Notes

- **Event Log** (design doc Section 16): a simple append-only table. Every significant state change in `resolveTick` emits a log entry. First-class feature, not debug output.
- **Fog of war:** enforced **server-side**. The API must return only what a given nation is allowed to see (own + adjacent + allied territories + world events). Never send full world state to the client and hide it in the UI — that is trivially inspected.
- **Trade routes as real objects:** per design doc 14A.6, store each route as a row with an explicit path through territory ids — not an abstract nation-to-nation link. This is what makes route interdiction addable later.
- **Mutable culture traits:** per design doc 7.5, store traits as mutable numeric values with drift rules, not constants.
- **Projected-delta UI:** the API needs a "preview" capability — given a proposed action, return its projected effects (Trust/unrest/Prestige deltas) without committing it. Design the action system so preview and commit share the same logic.
- **Auth is minimal:** 5 known players. A simple invite + session cookie is plenty. Do not build OAuth, email verification, password reset flows, etc. for v1.
- **Frontend design direction** is deliberately deferred — get the game *working* first; commit to a strong visual identity later, once screens exist to style.

---

## 8. Recommended Build Order for Claude Code

Do **not** build all systems at once. Build in phases; each phase should be working and tested before the next. Tell Claude Code to follow this order explicitly.

**Phase 0 — Skeleton.** Monorepo set up (`engine/`, `server/`, `web/`). Docker Compose running an empty app + Postgres. "Hello world" page served through the Cloudflare tunnel. Confirms the whole deployment pipeline works before any game logic exists.

**Phase 1 — The engine core, headless.** The `engine/` package: world-state types, the territory data-file loader, and `resolveTick` handling only the simplest systems — resource production into stockpiles and upkeep. **Placeholder numbers everywhere.** Plus the simulation harness (Section 6) that can fast-forward N ticks. Goal: prove the tick loop works and is testable. No UI, no DB yet.

**Phase 2 — Persistence & the real tick.** Wire `server/` to Postgres (Prisma schema for world state and queued actions). Load world from DB, run a real scheduled tick in a transaction, write back. Still minimal systems.

**Phase 3 — The map & basic play.** React + MapLibre frontend showing the territory map. The small ~40–60 territory data set. Players can log in, see their nation, and queue the simplest action. Two-phase day (Main / Preparation) and the Mandate budget.

**Phase 4 — Core systems, one at a time.** Add each design-doc system individually, tuning its numbers in the harness before moving on: Infrastructure → Culture & Unrest → Diplomacy/Treaties → Trade → War. Each is a self-contained addition to `resolveTick`.

**Phase 5 — AI nations & activity tiers.** Caretaker AI (defensive-only autopilot), AI nations, the inactivity system.

**Phase 6 — Prestige, Event Log polish, fog-of-war hardening, UI design pass.**

**Phase 7 — Scale the map.** Expand the territory data file from the small set to the full 200–500 real-world world. No engine changes — pure content.

---

## 9. Open Questions for This Document

- Fastify vs. Express — **decided: Fastify** (chosen in Phase 0).
- Whether the tick scheduler lives in-process (`node-cron`) or as an external system cron — decide in Phase 2.
- Initial continent(s) for the Phase 3 small territory set.
- Backup strategy for the Postgres volume (a persistent world's DB is irreplaceable — set up a simple periodic dump early, by Phase 2).

---

## 10. Build Decisions & Port Assignments

Recorded here as decisions are made. Each entry locks a choice so it is not revisited silently.

**Phase 0 decisions:**

- **Web app port: 42069.** The Vite dev server (web container) listens on 42069. This is the port the Cloudflare Tunnel's Zero Trust config points at.
- **Server API port: 3001.** The Fastify server listens on 3001, exposed on the host at 3001 for local debugging. The web container proxies `/api/*` to `http://server:3001` internally via the Vite proxy.
- **PostgreSQL:** internal to Docker Compose only (not exposed on the host). Access via `docker compose exec postgres psql -U war -d war`.
- **Named volume `war_pgdata`** backs the Postgres data directory. Survives `docker compose down` (without `-v`). Verified in Phase 0 go/no-go.
- **Web serving in development: Vite dev server** (separate `web` container). Hot-reload over the Docker bind-mount is the priority in early development.

**Phase 2 decisions:**

- **Tick scheduler: `node-cron` in-process** (chosen in Phase 2). The scheduler lives inside the Fastify server process and fires at midnight UTC-6. No external cron dependency — the scheduler starts with the server and respects the phase override for dev testing. This is the simplest path; move to an external trigger only if in-process scheduling proves unreliable at scale.

**Phase 4 decisions:**

- **Amphibious invasions: deferred to Phase 7** (map scale-up). Transport ships exist as a concept in the data model (`Nation.transportShips` field, default 0) but no transport actions or amphibious penalty logic in v1 War. Architecture must not assume land-only movement — leave hooks for sea crossing. Penalty values remain [OPEN] until Phase 7 (see design doc §9.4 and §18).

**Deferred task — switch web serving to static build:**
> In a future hardening step (no earlier than Phase 3, before any public deployment), replace the Vite dev server container with a production static build served directly from `server/`. The Vite proxy and separate `web` container will be retired at that point. Do not do this until explicitly decided — the current setup is intentional.

---

## 11. Deferred Security Hardening

Items **intentionally deferred** until before any real-world or wider-audience deployment. All are marked with `[DEFERRED SECURITY]` in source comments.

| Item | Current (dev) value | Required before prod |
|---|---|---|
| `ADMIN_KEY` | `dev-only-insecure-key` | Real secret in env; rotate on any exposure |
| `SESSION_SECRET` | `dev-only-session-secret-change-before-prod` | ≥32-char random secret in env; never committed |
| Player credentials | Plaintext in `server/src/auth.ts` | Hashed + salted; stored outside the repo |
| Cookie transport | HTTP (signed, not encrypted) | HTTPS-only; `secure: true` on cookie |
| `POST /admin/set-phase` | Dev-only endpoint bypasses real clock | **Remove entirely** (or gate on `NODE_ENV !== 'production'`) |
| `/api/admin/*` endpoints | Key-gated; key typed into browser | **Disable entirely** before public deployment; never forward to a public host |
| `/admin` route in web bundle | Ships in the same bundle as player UI | Move to a separate build or gate behind a build-time flag; players can visit `/admin` and see the key prompt |
| `/api/dev/*` endpoints | Session-gated to `nation_costa_rica` | Remove or replace with real RBAC |

**How to harden (checklist for future session):**
1. Generate `SESSION_SECRET` with `openssl rand -hex 32`, set it in the server's env (Docker Compose or `.env` — add `.env` to `.gitignore`).
2. Change `ADMIN_KEY` to a random value, set in env.
3. Move player passwords out of `auth.ts`; load from env vars or a secrets file.
4. Enable HTTPS (Cloudflare Tunnel already terminates TLS — the issue is cookie `secure` flag + SameSite policy when behind the tunnel).
5. Disable `/api/admin/*` and `/api/dev/*` server routes entirely (or guard with `NODE_ENV`).
6. Remove the `AdminApp` import and `/admin` routing from `main.tsx` (or compile it out with an env flag).

---

## 13. Deferred Architecture Tasks

Items that are explicitly **not** acted on yet. Each has a trigger condition — do not address before that trigger.

| Task | What | Trigger |
|---|---|---|
| **Action-handler registry / Command Pattern** | ~~Extract per-action validation out of the monolithic `/api/action` endpoint into separate handler files (one per action type), registered in a map.~~ **Resolved in v0.7.** `server/src/actions/` with uniform `ActionHandler` interface (`validate`/`queue`); `/api/action` delegates to registry. | ~~Start of the Diplomacy sub-phase~~ — **Done.** |
| **Immer (or equivalent) for engine immutability** | ~~The engine currently clones state with shallow spread. Immer's `produce()` would make immutability structural rather than manual.~~ **Resolved in v0.7.** `produce()` in `resolveTick`; direct draft mutations throughout. | ~~Start of the Diplomacy sub-phase~~ — **Done.** |
| **Move heavy compute outside the tick transaction** | Currently `resolveTick` (pure CPU) runs inside the Prisma transaction, holding a DB connection and row locks for its entire duration. Fine for the current ~10-territory scale. At larger scale, this adds latency to the lock window for no benefit — the computation should happen before the transaction opens, with only the final writes inside it. | **Phase 7 (map scale-up)**, when territory count grows past ~100. |

---

## 12. Change Log

- **v0.29** — Phase 6.5 embassy system + harness validation. Engine (`engine/src/types.ts`): `EmbassyStatus` union (`proposed | under_construction | active | expelled | destroyed`); `Embassy` interface (`id, ownerNationId, hostTerritoryId, status, constructionTicksLeft, startedAtTick`); `WorldState.embassies: Embassy[]`; `TerritoryState.lastEquilibriumCauses?: UnrestCauses` written each tick; `VisEmbassyInput` in visibility inputs. Engine (`engine/src/culture.ts`): `EMBASSY_COMPAT_BONUS = −0.01`, `EMBASSY_TRUST_RECOVERY_PER_TICK = 0.5`, `EMBASSY_EXPEL_TRUST_PENALTY = −10` constants [all PLACEHOLDER]. Engine (`engine/src/visibility.ts`): `VisEmbassyInput` type; embassy grants (Rule 4c) in `computeVisibility` — active embassy owned by observer in host territory → Clear. Engine (`engine/src/tick.ts`): `EMBASSY_BUILD_TICKS = 3`; `build_embassy` action case (proposed → under_construction, constructionTicksLeft=EMBASSY_BUILD_TICKS); embassy construction-tick decrement in territory loop (under_construction → active when 0); `expel_embassy` action case (active → expelled, host Trust penalty); destroyed on ownership change; `lastEquilibriumCauses` written to territory state after `computeUnrestEquilibrium`; `|| true` cession stub removed. Engine (`engine/src/loader.ts`): `embassies: []` default on WorldState. Server (`server/prisma/schema.prisma`): `Embassy` model (`id`, `ownerNationId`, `hostTerritoryId`, `status`, `constructionTicksLeft`, `startedAtTick`). DB migration `20260605020000_embassy`. Server (`server/src/actions/proposeEmbassy.ts`, `buildEmbassy.ts`, `expelEmbassy.ts`): NEW — three embassy action handlers; `proposeEmbassy` creates Embassy row; `buildEmbassy` sets status=under_construction; `expelEmbassy` sets status=expelled. Server (`server/src/actions/index.ts` + `phase.ts`): embassy handlers registered; costs: propose 1 Mandate, build 1 Mandate, expel 1 Mandate (all [PLACEHOLDER]). Server (`server/src/world.ts`): loads/saves Embassy rows; destroys embassy on ownership change (sets status=destroyed). Server (`server/src/index.ts`): embassy grants passed to `computeVisibility` in `/api/world`; propose/build/expel embassy Mandate discounts wired. Harness (`harness/src/types.ts`): `ActionType` gains `propose_embassy`, `build_embassy`, `expel_embassy`, `set_trade_route`, `assert_equilibrium_component`; `EquilibriumComponentAssertionError` interface; `AssertionError` discriminated union updated; `RunResult.assertionErrors: AssertionError[]`. Harness (`harness/src/runner.ts`): `propose_embassy` handler (direct state injection into `world.embassies`); `build_embassy`/`expel_embassy` engine pass-throughs; `set_trade_route` handler (injects TradeRoute into `world.tradeRoutes`); `assert_visibility` pass updated to include active embassy data; `assert_equilibrium_component` post-tick pass (reads `lastEquilibriumCauses`, checks named component present/absent). Five new scenarios: `trade-integration.json` (12t), `cultural-constraints.json` (13t), `movement-travel.json` (6t), `embassy-lifecycle.json` (10t), `territory-cession.json` (12t). All 28 harness scenarios pass; all 23 pre-existing scenarios byte-identical.
- **v0.28** — Phase 6.5 movement model + diplomatic value engine. Engine (`engine/src/types.ts`): `Army` gains `transitPath: string[]`, `transitTicksRemaining: number`; `TerritoryModifier` interface; `BorderSkirmish` interface; `WorldState` gains `territoryModifiers: TerritoryModifier[]`, `borderSkirmishes: BorderSkirmish[]`. Engine (`engine/src/war.ts`): `BASE_MOVEMENT_TICKS`, `GEOGRAPHY_MOVEMENT_MODIFIER` map, `ROAD_MOVEMENT_MODIFIER`, `BARRICADE_DEFENSE_BONUS`, `BARRICADE_MOVEMENT_MULTIPLIER`, `BARRICADE_DURATION_TICKS`, `SKIRMISH_FULL_CB_WINDOW`, `SKIRMISH_CB_DECLARATION_WINDOW`, `SKIRMISH_HOSTILITY_COMPAT_THRESHOLD` constants [all PLACEHOLDER]; `computeTerritoryTravelCost` and `computeArmyPath` pure functions. Engine (`engine/src/diplomaticValue.ts`): NEW — `computeClauseWealthValue`, `computeClauseDiplomaticWeight`, `computeMinCollateral`, `maintainPeaceTrustMultiplier`; `COLLATERAL_FLOOR_RATE`, `MAINTAIN_PEACE_LOW_VALUE_TRUST_FRACTION`, `MAINTAIN_PEACE_MAX_CONSECUTIVE`, `MAINTAIN_PEACE_CONSECUTIVE_WINDOW` constants [all PLACEHOLDER]. Engine (`engine/src/tick.ts`): `move_army` case replaced with multi-tick transit initiation; transit advancement block (one step per tick); `build_barricade` case (creates TerritoryModifier); border skirmish detection block; TerritoryModifier expiry filter; modifier `unrestEquilibriumAdj` + `driftRateMultiplier` applied in territory loop. Engine (`engine/src/loader.ts`): `transitPath: []`, `transitTicksRemaining: 0` defaults on Army; `territoryModifiers: []`, `borderSkirmishes: []` on WorldState. Engine (`engine/src/index.ts`): exports `'./diplomaticValue'`. Server (`server/src/actions/moveArmy.ts`): full multi-hop path validation via `computeArmyPath`; military-access check per foreign territory along path; in-transit guard. Server (`server/src/actions/buildBarricade.ts`): NEW — validates ownership, queues `build_barricade`. Server (`server/src/actions/index.ts`): `buildBarricadeHandler` registered. Server (`server/src/phase.ts`): `build_barricade: 1` in `ACTION_COSTS` + `ACTION_PHASE`. Server (`server/src/actions/proposeTreaty.ts`): `MAINTAIN_PEACE_MAX_CONSECUTIVE` + `MAINTAIN_PEACE_CONSECUTIVE_WINDOW` imported; consecutive maintain_peace limit enforced via `TreatyHistory` count query. Server (`server/src/actions/acceptTreaty.ts`): writes `TreatyHistory` row per maintain_peace objective clause on signing. Server (`server/src/index.ts`): imports `computeClauseWealthValue`, `computeClauseDiplomaticWeight`, `computeMinCollateral`, `maintainPeaceTrustMultiplier`; `POST /api/treaty/preview` session-gated endpoint. Server (`server/src/world.ts`): loads `transitPath`/`transitTicksRemaining` on armies; loads/saves `TerritoryModifier` + `BorderSkirmish` rows via `tx_any`. Server (`server/prisma/schema.prisma`): `Army.transitPath`, `Army.transitTicksRemaining`; `TerritoryModifier` model; `BorderSkirmish` model; `TreatyHistory` model. DB migration `20260605010000_movement_model`. Harness (`harness/src/runner.ts`): `transitPath: []`, `transitTicksRemaining: 0` on seeded armies. All 23 harness scenarios byte-identical.
- **v0.27** — Phase 6.5 treaty system expansion. Engine (`engine/src/types.ts`): `ClauseType` union gains `territory_cession`, `army_lending`, `population_transfer`, `outpost`; `UnrestCauses` gains `populationTransferShock`; `TerritoryState` gains `hasEmbassy: boolean` and `populationTransferShockTicksLeft: number`. Engine (`engine/src/culture.ts`): `POPULATION_TRANSFER_UNREST_SCALE`, `POPULATION_TRANSFER_SHOCK_DURATION`, `POPULATION_TRANSFER_DRIFT_DURATION`, `CESSION_EMBASSY_GRACE_TICKS`, `CESSION_MIN_FUTURE_TICKS`; `computeUnrestEquilibrium` gains `populationTransferShock` param. Engine (`engine/src/loader.ts`): `initialState` defaults new fields. Engine (`engine/src/tick.ts`): tribute auto-assign (proportional local stockpile deduction); trade auto-assign (null `sourceTerritoryId` path); `territory_cession` evaluation (embassy stub, grace period, breach); `population_transfer` evaluation (one-time transfer, shock); `army_lending` evaluation (war revoke, return penalty); `outpost` clause ownership-change degradation; `populationTransferShockTicksLeft` decrement in territory loop. Engine (`engine/src/visibility.ts`): `VisTreatyInput.outpostGrants`; outpost tier lookup in `computeVisibility` Rule 4b. Server (`server/src/index.ts`): outpost grants populated in `visTreaties` builder. Server (`server/src/actions/proposeTreaty.ts`): 4 new clause types validated. Server (`server/prisma/schema.prisma`): `TerritoryState.hasEmbassy`, `TerritoryState.populationTransferShockTicksLeft`. DB migration: `20260605000000_treaty_expansion`. All 23 harness scenarios byte-identical.
- **v0.26** — Phase 6.5 initialization pipeline. Engine (`engine/src/initialization.ts`): NEW — `deriveTerritoryTraits`, `deterministicSeed`, `lcgRng`, `FAMILY_TRAIT_OFFSETS`, `GEOGRAPHY_TRAIT_MODIFIERS`, `GEOGRAPHY_BASE_POPULATION`, `FAMILY_POPULATION_MULTIPLIER`, `FAMILY_PRODUCTION_MODIFIERS`, `TRAIT_RNG_VARIANCE`; `ProductionModifiers` interface; all exported via `engine/src/index.ts`. Engine (`engine/src/types.ts`): `TerritoryDef.traitOverrides` optional field. Server (`server/src/world.ts`): imports `deriveTerritoryTraits`, `deterministicSeed`; `ensureWorldInitialized` applies pipeline at world init. Server (`server/src/index.ts`): imports both; `GET /api/admin/territory/:id/derived-traits` endpoint with optional `?geography=` override. `docs/dev-commands.md` §17. `docs/tuning-notes.md`: pipeline constants documented. All 23 harness scenarios byte-identical.
- **v0.25** — Phase 6.5 systems integration. Engine (`engine/src/types.ts`): `UnrestCauses` gains 7 new named components: `tradeStability`, `isolationistEntanglement`, `expansionistStagnation`, `collectivistIsolation`, `individualistObligation`, `traditionalErosion`, `progressiveStagnation`. Engine (`engine/src/culture.ts`): new constants `TRADE_STABILITY_BONUS`, `TRADE_DRIFT_MULTIPLIER`, `ISOLATIONIST_TREATY_THRESHOLD`, `ISOLATIONIST_ENTANGLEMENT_WEIGHT`, `EXPANSIONIST_GROWTH_WINDOW`, `EXPANSIONIST_STAGNATION_WEIGHT`, `COLLECTIVIST_ISOLATION_WEIGHT`, `INDIVIDUALIST_OBLIGATION_WEIGHT`, `TRADITIONAL_EROSION_THRESHOLD`, `TRADITIONAL_EROSION_WEIGHT`, `PROGRESSIVE_STAGNATION_THRESHOLD`, `PROGRESSIVE_STAGNATION_WEIGHT`, `ROAD_DRIFT_MULTIPLIER`; `computeUnrestEquilibrium` gains 7 new optional parameters; `applyDrift` gains optional `driftMultiplier` parameter. Engine (`engine/src/war.ts`): `GEOGRAPHY_SHOCK_MULTIPLIER` map (mountainous/forest/desert/island/coastal/plain). Engine (`engine/src/trade.ts`): `CAPACITY_BASE`, `SEA_CAPACITY_MULTIPLIER`, `LAND_ROAD_CAPACITY_MULTIPLIER`, `NO_INFRA_CAPACITY_MULTIPLIER`, `FRICTION_BASE`, `FRICTION_MOUNTAIN`, `FRICTION_DESERT`, `FRICTION_HOSTILE_CROSSING`, `FRICTION_ROAD_REDUCTION`; `computeTradeCapacity` and `computeTradeFriction` pure functions. Engine (`engine/src/tick.ts`): `POPULATION_PRODUCTION_BASE` constant; trade route territory map pre-computation for tradeStability + trade drift; per-nation pre-computations for 6 new cultural constraint axes (tribute obligations, latest acquisition tick, active treaty count reuse); three conquest shock callsites updated with geography multiplier; production loop scaled by `(basePopulation / POPULATION_PRODUCTION_BASE)`; gross wealth scaled accordingly; `ROAD_DRIFT_MULTIPLIER` applied to both unrest and culture drift; `TRADE_DRIFT_MULTIPLIER` applied to culture drift for route territories; imports updated. Server (`server/src/actions/acceptTreaty.ts`): imports `computeTradeCapacity` and `computeTradeFriction`; populates `capacity` and `friction` on TradeRoute at treaty signing. `docs/tuning-notes.md`: all new constants documented with first-pass observations. All 23 harness scenarios pass.
- **v0.24** — Phase 6 hardening. Engine (`engine/src/war.ts`): `DEBT_RECOVERY_SKIM_RATE` 0.20→0.30; comment updated to document gross-production skim. Engine (`engine/src/tick.ts`): `grossWealthByNation` map replaces `incomingWealthByNation` in debt recovery skim block; `grossWealthByNation` computed from non-revolting owned territory `baseWealth` sums before upkeep. Harness (`harness/src/types.ts`): `ActionType` union adds `assert_visibility`; `AssertionError` interface; `RunResult.assertionErrors: AssertionError[]`. Harness (`harness/src/runner.ts`): imports `computeVisibility`, `VisibilityTier`; `assertionErrors` array; `assert_visibility` action skipped in pre-tick loop; post-tick assertion pass calls `computeVisibility` with full world state, compares `actualTier` vs `expectedTier`, pushes to `assertionErrors` on mismatch; army seeding filter adds `a.size > 0` guard. Harness (`harness/src/report.ts`): `⚠ ASSERTION FAILURES` section in `report.md` when `assertionErrors.length > 0`. Harness (`harness/src/cli.ts`): `process.exit(1)` on assertion failures. Scenarios: `fog-of-war.json` (3 nations, 5 ticks, 6 `assert_visibility` actions at T1+T5, `armySize: 0` for observer nation). `scenarios/war-exhaustion.json`: description updated for v0.24 gross-skim fix. `docs/persistent-world-design.md`: stale comment report included in v0.24 entry.
- **v0.23** — War council. Server (`server/src/council.ts`): NEW — `createWarCouncils`, `addNationToDefenderCouncil`, `mirrorMilitaryAction` helpers. Server (`server/src/actions/declareWar.ts`): imports + calls `createWarCouncils` after war row is created. Server (`server/src/actions/attackTerritory.ts`, `moveArmy.ts`, `retreatArmy.ts`): each imports + calls `mirrorMilitaryAction` inside queue transaction. Server (`server/src/tick.ts`): imports council helpers; `addNationToDefenderCouncil` called for defense pact allies + `createWarCouncils` for auto-war; `councilQueuedAction.deleteMany()` after tick; `warCouncil.deleteMany` for ended wars; all three reset-world variants add `warCouncil.deleteMany()`. Server (`server/src/index.ts`): `GET /api/war/:warId/council` — loads council, verifies side membership, returns members/actions/contested territories/joint invasion objectives; `GET /api/world` adds `myActiveWarIds` query + field. Server (`server/prisma/schema.prisma`): `WarCouncil` + `CouncilQueuedAction` models. DB migration: `20260603020000_war_council`. Web (`web/src/api.ts`): `CouncilMember`, `CouncilContestedTerritory`, `JointInvasionChecklist`, `WarCouncilView` interfaces; `api.warCouncil(warId)`; `WorldView.myActiveWarIds`. Web (`web/src/components/WarCouncilPanel.tsx`): NEW — fixed bottom bar with three-column layout; only renders when `world.myActiveWarIds` is non-empty; polls council endpoint via `useEffect` on world changes. Web (`web/src/App.tsx`): imports + mounts `WarCouncilPanel`.
- **v0.22** — Prestige formula + Dominant. Engine (`engine/src/prestige.ts`): NEW — full `computePrestige` formula + `computeDominantNations`; all constants [PLACEHOLDER]; exported via index. Engine (`engine/src/types.ts`): Nation fields `completedTreatiesKept`, `warsWon`, `foundedAtTick`, `isDominant`. Engine (`engine/src/loader.ts`): defaults for new fields. Engine (`engine/src/tick.ts`): imports `DOMINANT_WAR_ATTACKER_BONUS`, `DOMINANT_WAR_MILITARISTIC_BONUS`; `completedTreatiesKept` incremented at natural expiry + early-complete; `warsWon` incremented in `executePeaceDeal`; Dominant attacker bonus in battle; Dominant Militaristic unrest bonus in war-unrest loop. Server (`server/src/actions/breakTreaty.ts`): `DOMINANT_TRUST_PENALTY_REDUCTION` applied. Server (`server/src/actions/acceptTreaty.ts`): `UNDERDOG_PRESTIGE_BONUS` applied; `underdogBuffExpiresAt` column scaffolded. Server (`server/src/world.ts`): imports `computePrestige`, `computeDominantNations`; loads/saves new Nation fields; stub Prestige block replaced; writes `PrestigeHistory`; `ensureWorldInitialized` sets `foundedAtTick`. Server (`server/src/caretaker.ts`): fragmentation-spawned nations get `foundedAtTick`. Harness (`harness/src/runner.ts`): fragmentation-spawned nations get `completedTreatiesKept/warsWon/foundedAtTick/isDominant`. DB migration: `20260603010000_prestige_full`. Server (`server/prisma/schema.prisma`): Nation columns + `PrestigeHistory` model. Server (`server/src/index.ts`): fetches `prestigeHistory` (last 20 ticks); nations response gains `prestigeDelta/isDominant/prestigeHistory/completedTreatiesKept/warsWon`. Web (`web/src/api.ts`): `PrestigeHistoryPoint`, `NationView` extended. Web (`web/src/components/PrestigeLeaderboard.tsx`): full rebuild with `Sparkline` SVG component, delta label, Dominant ★ badge, hover secondary stats. Web (`web/src/App.tsx`): passes `currentTick` to `PrestigeLeaderboard`.
- **v0.21** — Fog of war. Engine (`engine/src/visibility.ts`): NEW — `VisibilityTier` const enum (TrueFog=0, LightFog=1, Clear=2); `computeVisibility(input: ComputeVisibilityInput): Map<string, VisibilityTier>` pure function; input types `VisTerritoryInput`, `VisArmyInput`, `VisTreatyInput`, `VisFederationInput`; rules: own→Clear, army present/adj→Clear, military_access→Clear [PLACEHOLDER], federation→Clear, any treaty→LightFog, adjacent to own→LightFog, default→TrueFog. Exported from `engine/src/index.ts`. Server (`server/prisma/schema.prisma`): `Federation` model (`id`, `name`, `foundedAtTick`, `status`) and `FederationMember` model (`federationId`, `nationId`, `joinedAtTick`, `role`); `@@unique([federationId, nationId])`. DB migration: `20260603000000_federation_fog`. Server (`server/src/index.ts`): `/api/world` imports `computeVisibility`, `VisibilityTier`, `ComputeVisibilityInput`, `VisTreatyInput`; parallel-fetches `armyRows`, `treatyRows` (with clauses), `federationRows`; builds `ComputeVisibilityInput` from DB data; calls `computeVisibility`; filters territory response into three shapes by tier (TrueFog/LightFog/Clear); armies grouped by territory and included in Clear responses; `/api/admin/world-full` unchanged (full data). `POST /api/admin/create-federation` endpoint: body `{name, memberNationIds}`, creates `Federation` + `FederationMember` rows. Web (`web/src/api.ts`): `VisibilityTier` const object; `VisibilityTierValue` type; `TerritoryArmyView` interface; `TerritoryView` rebuilt with `visibilityTier` field and tier-gated optional fields; `api.admin.createFederation`. Web (`web/src/components/GameMap.tsx`): `desaturate()` helper; `TRUE_FOG_COLOR` constant; `enrichGeojson` computes `fillColor`/`fillOpacityBase`/`labelVisible`/`visibilityTier` per feature; fill layer uses `['get', 'fillOpacityBase']`; outline layer uses tier-conditional color + opacity. Web (`web/src/components/InfoPanel.tsx`): TrueFog guard (geography + "outside range" message); LightFog guard (owner name + "limited intelligence" message); Clear falls through to existing full panel. `docs/dev-commands.md`: §16 federation admin commands.
- **v0.20** — Army positioning. Engine (`engine/src/types.ts`): `Army` interface (`id`, `nationId`, `territoryId`, `size`, `status`, `destinationTerritoryId`, `movedThisTick`); `TerritoryClaim` interface; `WorldState.armies` + `WorldState.territoryClaims`; `Nation.armySize` deprecated with `@deprecated` JSDoc. Engine (`engine/src/war.ts`): `PACIFICATION_THRESHOLD=100`, `PACIFICATION_DECAY_PER_TICK=10`, `TERRAIN_DIFFICULTY` map, `POP_DIFFICULTY_SCALE`, `COMPAT_DIFFICULTY_SCALE` (all [PLACEHOLDER]); `totalArmySize`, `armyInTerritory`, `armiesForNation` helpers. Engine (`engine/src/tick.ts`): `move_army` action case (adjacency check, movedThisTick guard, war→besieging logic, military_access enforcement — [DEFERRED] stub removed); `claim_territory` action case (unclaimed + adjacency + no-duplicate check, TerritoryClaim upsert); siege-by-army-presence block (besieging armies auto-advance each tick without re-queuing); pacification resolution block (army present → progress += size/difficulty; absent → progress -= DECAY; threshold → ownership transfer + conquest shock); army `movedThisTick` reset each tick. Server (`server/src/actions/moveArmy.ts`): NEW — validate adjacency + movedThisTick + military_access check; queue `move_army`. Server (`server/src/actions/claimTerritory.ts`): NEW — validate unclaimed + adjacency + no existing claim; create `TerritoryClaim` row at queue time; queue `claim_territory`. Server (`server/src/actions/index.ts`): both handlers registered. Server (`server/src/world.ts`): `loadWorldState` loads armies + territory claims; `saveWorldState` upserts/deletes armies and updates claim progress; `ensureWorldInitialized` seeds one Army per nation at capital. Server (`server/src/caretaker.ts`): defense priority replaced — BFS `bfsNextStep` to find next territory toward besieged territory; queues `move_army` (no teleporting); fragmentation-spawned AI nations get Army row. Server (`server/src/ai.ts`): `expand_claim` split into two steps — Step 1: queue `claim_territory` + create `TerritoryClaim` row; Step 2: queue `move_army` toward claimed territory via BFS; `bfsNextStepAi` helper. Server (`server/src/index.ts`): all three reset-world variants delete Army + TerritoryClaim rows before re-init; `POST /api/admin/nation/:nationId/set-army` endpoint (create/replace first army, size=0 deletes all). Web (`web/src/api.ts`): `AdminArmyRow` interface; `AdminNationRow.armies: AdminArmyRow[]`. Web (`web/src/AdminApp.tsx`): nations table Army column shows per-army position, size, and status (colour-coded by status). Harness (`harness/src/types.ts`): `set_army_size` + `move_army` + `claim_territory` action types. Harness (`harness/src/runner.ts`): `set_army_size` handler (updates first army size or creates one); `move_army` handler (engine pass-through with armyId lookup); armies seeded per nation from `armySize` at scenario start. `docs/dev-commands.md`: §15 army admin commands. DB migration: `20260602040000_army_positions`. All 22 existing harness scenarios pass byte-identical.
- **v0.1** — Initial tech stack and build plan. Stack: TypeScript / Node / Fastify / PostgreSQL / Prisma / React / Vite / Tailwind / MapLibre GL JS, Docker Compose, self-hosted via Cloudflare Tunnel. Established the engine-as-pure-package principle, the territory-as-data-file principle, and the 8-phase build order. Map data via Natural Earth.
- **v0.2** — Phase 0 built. Port assignments locked (web: 42069, API: 3001). Fastify chosen. Named volume `war_pgdata` established. Deferred task logged: switch web to static build served from server/ before production hardening.
- **v0.3** — Phase 3 built. MapLibre GL JS map with Natural Earth Central America geodata. 5-player auth (signed cookies). Fog-of-war API. Two-phase day (Main until 19:00 CR, Prep until midnight). Mandate budget. `build_road` action end-to-end. Deferred security hardening checklist in §11.
- **v0.5** — Phase 4 Infrastructure. `build_port` and `build_fort` actions in engine and server. Strict single construction slot per territory. Multi-tick construction state (`constructionType`, `constructionTicksLeft`). Next-build pre-queue with mandate+industry pre-deducted and cancel-refund. `resolveTick` returns explicit per-action `ActionResult`. `engine/` baked into server image. Deferred architecture tasks logged in §13. All build times and costs tagged `[PLACEHOLDER]`.
- **v0.6** — Phase 4 Culture & Unrest. Value traits on ±1 scale. Cultural families + family-closeness table. Named unrest equilibrium components (base, compat clash, distance, infrastructure investment, empire size, conquest shock, rapid expansion, military stub). Conquest shock: compat-scaled initial value, infra-gated decay with integration-progress formula. Rapid-expansion smooth linear decay window. Capital 2× culture weight. Mandate decoupled from stockpiles → territory-development formula. `ownershipShock` and `acquiredTick` fields on `TerritoryState`. DB migrations for all new columns. Admin panel at `/admin` (admin-key gated) with god's-eye view, all territory attributes editable inline. Simulation harness (`npm run scenario` / `npm run sweep`) with markdown reports, per-tick CSVs, and PNG charts; three seed scenarios as regression baseline. Action-causal recovery principle empirically validated in belize-neglect vs belize-integrate contrast.
- **v0.7** — Pre-Diplomacy structural refactors (both deferred tasks from §13 now resolved). Action-handler registry: `server/src/actions/` with `types.ts` (uniform `ActionHandler` interface — `validate`/`queue`) and one handler file per action type; `/api/action` route reduced to registry lookup + mandate check. Immer adopted in `engine/`: `immer` added as engine dependency, `produce()` in `resolveTick` replaces all manual spread cloning. Both changes verified behavior-preserving: three harness scenarios byte-identical to pre-refactor baseline.
- **v0.8** — Diplomacy sub-phase. Schema: `Proposal`, `ProposalClause`, `Treaty`, `TreatyClause`, `TreatyParty` models; `Nation.inactivityTier` + `lastBrokenPromiseTick`. Engine: `diplomacy.ts` (Trust constants, proposal expiry, tribute transfers, Trust fines/recovery, treaty degradation, escrow refund, cultural-clash unrest); `WorldState.treaties` + `WorldState.proposals`; `UnrestCauses.treatyCulturalClash`; diplomacy resolved inside `produce()` block of `resolveTick`. Server: five action handlers (`proposeTreaty`, `acceptTreaty`, `declineTreaty`, `breakTreaty`, `proposeRenewal`); `GET /api/diplomacy`; admin endpoints (`/api/admin/diplomacy`, `set-trust`, `set-tier` with degradation/upgrade mechanics, `force-break`). Web: `DiplomacyPanel` (toggle button, proposal forms, treaty list, Trust scoreboard); admin Diplomacy section (treaty inspector, nation Trust/tier controls). Migration: `20260529_diplomacy_phase`.
- **v0.9** — Trade sub-phase (Prompt 1). Schema: `InstantTrade`, `TradeRoute` models; `TerritoryState` local stockpile columns (`localPopStock`, `localIndStock`, `localWltStock`). Engine: `trade.ts` (`findTradePath` BFS + sea-route shortcut, `isPathStale`, local stockpile field helpers); `resolveTick` resolves instant-trade accept/decline/expiry; trade clause flows deduct from nation general stockpile each tick; missed-payment breach after 2 consecutive misses; clause degradation on source territory loss. Server: `instantTrade` and `acceptInstantTrade`/`declineInstantTrade` action handlers; `acceptTreaty` computes and stores `TradeRoute` at signing. Web: instant trade send/accept/decline UI in `DiplomacyPanel`; source territory local stock display for trade source selection. Migration: `20260529_trade_phase`.
- **v0.10** — Trade sub-phase (Prompt 2): Objective clauses. Schema: `ObjectiveClause` model (one-to-one with `TreatyClause`). Engine types: `ObjectiveClause`, `ObjectiveType`, `ObjectiveStatus`, `ResponsibleParty`; `TreatyClause.objective` field; `ClauseType` union extended with `'objective'`. Engine logic: per-tick objective evaluation in `resolveTick` — functional types `build_port`, `build_road_connection`, `maintain_peace`; stub types `joint_invasion`, `attack_player` (data model present, inert). `diplomacy.ts`: `objectiveMeetBonus`, `responsibleNationIds`, `hasRoadConnectionToTerritory`, `breachMaintainPeaceObjectives` (War integration hook). Server: objective validation in `proposeTreaty`; `ObjectiveClause` row created in `acceptTreaty`; status persisted in `saveWorldState`; admin `force-meet`/`force-fail` endpoints; objective data included in `/api/diplomacy` and `/api/admin/diplomacy`. Web: `api.ts` types (`ObjectiveClauseView`, `ObjectiveType`, etc.); `DiplomacyPanel` objective clause display with countdown and plain-language description; propose-treaty objective clause builder; admin treaty table with objective sub-rows and force buttons. Harness: `TreatySnapshot.objectives`; `ObjectiveSnapshot` type; report timeline for objective status; two new scenarios (`objective-port-met`, `objective-port-failed`); all 8 scenarios pass. Migration: `20260601000000_objective_clause`.
- **v0.19** — Phase 5 harness scenarios. Harness (`harness/src/runner.ts`): `applyHarnessCaretaker` (road build + expansion for Dormant/Autopilot); `applyHarnessFragmentation` (tick-based fragmentation risk, harness constants `HARNESS_ABANDON_TIME_SCALE_TICKS=10`, `HARNESS_ABANDON_FRAGMENT_THRESHOLD=0.6`); `harnessFragmentationRisk` exported; `set_nation_tier` also sets `activityTier`; `abandonedAtTickByNation: Map<string,number>` side map; `captureSnapshot` accepts the map; calls `applyHarnessCaretaker` + `applyHarnessFragmentation` before AI pass. Harness (`harness/src/types.ts`): `NationDiplomacySnapshot.activityTier`; `TerritoryFragmentationSnapshot` interface; `TickSnapshot.fragmentationData`. Harness (`harness/src/snapshot.ts`): imports `harnessFragmentationRisk`; captures `activityTier` and `fragmentationData`. Harness (`harness/src/report.ts`): `fragmentation-risk.csv` output; `activity_tier` column in `nation-diplomacy.csv`; chart list entries; `hasFragData` inside `generateCSVs`, `hasFragDataForChart` inside `generateReport`. Harness (`harness/scripts/charts.py`): `chart_activity_tier_over_time`, `chart_fragmentation_risk_over_time`; loads `fragmentation-risk.csv`; calls both from `main()`. Scenarios: `caretaker-roads.json`, `abandonment-fragmentation.json`, `fragment-becomes-ai.json`, `ai-expansionist.json` (15t), `ai-merchant.json` (15t). `docs/harness.md` updated with new scenarios, `fragmentation-risk.csv` doc, chart table entries.
- **v0.18** — AI doctrine system. Engine (`engine/src/doctrine.ts`): NEW — `deriveDoctrineBlend`, `scoreAction`, `AI_EFFICIENCY_PENALTY`, `OFFENSIVE_WAR_GATE`, `OFFENSIVE_WAR_THRESHOLD`, `BALANCED_DOCTRINE`; exported via `engine/src/index.ts`. Engine (`engine/src/types.ts`): `DoctrineBlend` interface; `Nation.doctrineBlend: DoctrineBlend | null`. Engine (`engine/src/loader.ts`): `doctrineBlend: null` on default nation. Server (`server/src/ai.ts`): NEW — `runAiNations`; queries AI nations, scores candidates, queues/applies actions; `findTreatyTarget` helper. Server (`server/src/tick.ts`): imports + calls `runAiNations` after caretaker. Server (`server/src/caretaker.ts`): imports + calls `deriveDoctrineBlend` when spawning fragmentation AI. Server (`server/src/world.ts`): loads/saves `doctrineBlend`. Server (`server/prisma/schema.prisma`): `Nation.doctrineBlend Json?`. Harness (`harness/src/types.ts`): `set_ai_doctrine` ActionType; `ScenarioNation.isAI`; `ScenarioWorld.autoAcceptTreaties`. Harness (`harness/src/runner.ts`): `set_ai_doctrine` handler; `autoAcceptTreaties` loop; `applyHarnessAiActions`; imports `scoreAction`, `BALANCED_DOCTRINE`, `PROPOSAL_EXPIRY_TICKS`. Scenarios: `ai-expansionist.json`, `ai-merchant.json`. DB: `doctrine_blend.sql`.
- **v0.17** — Activity tiers + caretaker AI. Server (`server/src/caretaker.ts`): NEW — tier transitions, caretaker action queuing, abandoned fragmentation, nation dissolution. Server (`server/src/tick.ts`): imports `runCaretaker`, calls it after `saveWorldState` and action-clear. Server (`server/src/index.ts`): stamps `lastActiveAt = now` + resets `activityTier = 'active'` on login and action queue; adds `POST /api/admin/nation/:id/set-tier`, `POST /api/admin/nation/:id/convert-to-ai`; exposes `activityTier/lastActiveAt/abandonedAt` in `admin/world-full` nations; exposes `fragmentationRisk` per territory in `admin/world-full`. Engine (`engine/src/types.ts`): `Nation.activityTier: string`, `Nation.caretakerPriorities: string[]`. Engine (`engine/src/loader.ts`): both fields on default nation. Server (`server/src/world.ts`): loads/saves both fields. Server (`server/prisma/schema.prisma`): four new Nation fields. DB migration: `20260602030000_activity_tiers`. Web (`web/src/api.ts`): `AdminNationRow` gains `activityTier/lastActiveAt/abandonedAt`; `AdminTerritoryRow` gains `fragmentationRisk: number | null`; `api.admin.convertToAi`. Web (`web/src/AdminApp.tsx`): "→ AI" button for Abandoned nations; `Frag` column in territories table. `docs/dev-commands.md`: §14 activity tier admin commands.
- **v0.16** — Insolvency fix. Engine (`engine/src/types.ts`): `Nation.debtBalance: number`; `UnrestCauses.insolvencyPressure: number`. Engine (`engine/src/culture.ts`): `INSOLVENCY_GENERAL_UNREST_PER_TICK = 0.02`; `computeUnrestEquilibrium` gains 11th param `insolvencyPressure = 0` (backward-compatible). Engine (`engine/src/war.ts`): `DEBT_RECOVERY_SKIM_RATE = 0.20`. Engine (`engine/src/tick.ts`): removed `Math.max(0, ...)` floors on upkeep, tribute, low-Trust fines; added `incomingWealthByNation` tracking map during production flush; insolvency+debt resolution block after upkeep; `generalInsolventNations` set drives `insolvencyPressure` in territory loop; passes `insolvencyPressure` to `computeUnrestEquilibrium`; `warInsolventNations` now subset of `generalInsolventNations`. Engine (`engine/src/loader.ts`): `debtBalance: 0` on initial nation. Server (`server/prisma/schema.prisma`): `Nation.debtBalance Float @default(0)`. Server (`server/src/world.ts`): loads/saves `debtBalance`. Server (`server/src/index.ts`): exposes `debtBalance` and `isInsolvent` on own nation in `/api/world`; insolvency mandate surcharge (+1 on cost≥2 while insolvent). Web (`web/src/api.ts`): `NationView.debtBalance?`, `NationView.isInsolvent?`; `UnrestCauses.insolvencyPressure?`, `treatyCulturalClash?` (optional). Web (`web/src/components/InfoPanel.tsx`): red wealth when negative; INSOLVENT badge; debt recovery line; `insolvencyPressure` + `treatyCulturalClash` in unrest breakdown. Harness (`harness/src/types.ts`): `NationDiplomacySnapshot.debtBalance`. Harness (`harness/src/snapshot.ts`): captures `debtBalance`. Harness (`harness/src/report.ts`): `nation-diplomacy.csv` gains `debt_balance` column. Harness scenario `war-exhaustion.json`: updated description — insolvency ramp now fires correctly. Migration: `20260602020000_insolvency`.
- **v0.15** — War sub-phase (Prompt 4): Harness war scenarios. Harness (`harness/src/types.ts`): `WarSnapshot` type; `wars` on `TickSnapshot`; `armySize` on `NationSnapshot`; new `ActionType` entries: `declare_war`, `propose_peace`, `attack_territory`, `accept_peace`, `set_fort_level`. Harness (`harness/src/runner.ts`): handlers for all new action types; `declare_war` injects `War` into `world.wars` + no-CB Trust penalty; `propose_peace` mutates war to `peace_negotiation` + sets `pendingPeaceDeal`; `attack_territory`/`accept_peace` pass to `resolveTick` with explicit `nationId`. Harness (`harness/src/snapshot.ts`): captures `armySize` + `wars` each tick. Harness (`harness/src/report.ts`): emits `war-state.csv` (one row per active war per tick) and `army-sizes.csv`; references `war-state-over-time.png` in chart list. Harness (`harness/scripts/charts.py`): `chart_war_state_over_time` (3-panel: army sizes, occupied count, avg unrest); loaded from `war-state.csv` + `army-sizes.csv`; called from `main()`. Five new scenarios: `war-conquest`, `war-fortified`, `war-no-cb`, `war-exhaustion`, `war-defense-pact`. Docs: `harness.md` updated with new action types, output files, chart, and war scenario table. `tuning-notes.md`: war insolvency ramp gap documented. All 12 existing scenarios byte-identical. Tagged `phase-4-war-complete`.
- **v0.14** — War sub-phase (Prompt 3): Activate war stubs + Prestige stub. Engine (`engine/src/tick.ts`): `breachMaintainPeaceObjectives` moved from server queue-time to engine `declare_war` case — import added to `./diplomacy`; `breachMaintainPeaceObjectives` now called on `draft.treaties` so changes flow through `saveWorldState`; `joint_invasion` objective evaluation (both responsible parties must attack `targetTerritoryId` same tick); `attack_player` objective evaluation (responsible party is `attackerId` in any active war vs `targetNationId`); battle loop extended to include `peace_negotiation` wars (unchanged from Prompt 2, confirmed). Server (`server/src/tick.ts`): defense pact auto-defense block in `runTick` — scans `appliedWarDeclarations` from `actionResults`, queries defender treaties for active `defense_pact` clauses, creates `War` row + queued `declare_war` action + Event Log entry for each triggered ally; degraded-pact (non_aggression) path unchanged. Server (`server/src/actions/attackTerritory.ts`): military_access reachability check — if not directly adjacent, queries attacker's active `military_access` treaties, checks if any partner owns a territory adjacent to the target; `[DEFERRED: full movement model Phase 5]` tagged. Server (`server/src/world.ts`): Prestige computed in `saveWorldState` from post-save DB state — territory counts, treaty counts, avg unrest, war-win counts; `Nation.prestige` updated every tick. Web (`web/src/api.ts`): `NationView.prestige: number` added. Web (`web/src/components/PrestigeLeaderboard.tsx`): new component — fixed top-right panel, all nations ranked by prestige desc, own nation highlighted. Web (`web/src/App.tsx`): `PrestigeLeaderboard` mounted unconditionally. Server (`server/src/index.ts`): `nations` response includes `prestige` for all nations. All 12 harness scenarios byte-identical.
- **v0.13** — War sub-phase (Prompt 2): Peace negotiation. Engine types: `PeaceDeal`, `PeaceDealType`, `Territorycessation` in `engine/src/types.ts`; `War.exhaustionByNation` field. Engine constants (`engine/src/war.ts`): `PEACE_PROPOSAL_LAPSE_TICKS`, `PEACE_DECLINE_EXHAUSTION_BUMP`, `PEACE_DECLINE_EXHAUSTION_TICKS`, `PEACE_TRUST_BONUS` (all `[PLACEHOLDER]`). Engine logic (`engine/src/tick.ts`): peace resolution block — `executePeaceDeal` helper (territory cessions + conquest shock, territory returns, `[TRIBUTE_TREATY]` event, Trust bonus, war end); lapse check (`proposedAtTick + LAPSE_TICKS`); decline → exhaustion bump; battle resolution continues during `peace_negotiation`; `warExhaustionNations` set drives PEACE_DECLINE_EXHAUSTION_BUMP in territory loop. Three new action handlers: `proposePeace` (2 Mandate [PLACEHOLDER]; transitions War to `peace_negotiation` + sets `pendingPeaceDeal` in DB at queue time), `acceptPeace` (0 Mandate; non-proposer only), `declinePeace` (0 Mandate; non-proposer only). Server (`world.ts`): tribute-treaty creation on `[TRIBUTE_TREATY]` event log parse; ended-war `attack_territory` action cleanup. Admin: `POST /api/admin/force-peace` endpoint (force-accept with specified terms, tribunal treaty creation, Trust bonus). Schema: `War.exhaustionByNation JSONB NOT NULL DEFAULT '{}'`. Phase.ts: `propose_peace` 2 Mandate, `accept_peace`/`decline_peace` 0 Mandate. All 12 harness scenarios byte-identical. Migration: `20260602010000_peace_phase`.
- **v0.12** — War sub-phase (Prompt 1): declaration, army actions, battle resolution. Schema: `War` model with attacker/defender FK relations on `Nation`, `occupiedTerritories` as JSONB. Engine: `engine/src/war.ts` (battle formula, siege, overextension, all `[PLACEHOLDER]` constants); `engine/src/tick.ts` processes `attack_territory`/`retreat_army` actions; war-unrest block (overextension + insolvency + no-CB spike + militaristic happiness); `computeUnrestEquilibrium` in `culture.ts` extended with optional `militaryBonus` param; `militaryBonus` stub activated. Server: `declareWar`/`attackTerritory`/`retreatArmy` action handlers; `loadWorldState` + `saveWorldState` load/persist `War` rows; admin `declare-war`/`end-war` endpoints; dev-commands.md §13. Phase.ts: `declare_war` 3 Mandate, `attack_territory` 2 Mandate, `retreat_army` 0 Mandate ([PLACEHOLDER]). `engine/src/loader.ts`: `buildWorldState` includes `wars: []`. All 12 harness scenarios byte-identical. Migration: `20260602000000_war_phase`.
- **v0.11** — Trade sub-phase (Prompt 3): Harness scenarios and charts for trade + objective validation. Harness: `TreatySnapshot.tradeClauses` (new field — per-clause `missedPayments`, `clauseStatus`, and payload fields per tick); `TradeClauseState` type in `harness/src/types.ts`. New CSVs emitted by `generateCSVs`: `trade-flows.csv` (per-tick per-clause flow status inferred from consecutive `missedPayments` diffs), `objective-metrics.csv` (per-tick objective status for chart rendering). `charts.py`: new `chart_trade_flow_over_time` function (flow-status bar panel + Wealth divergence panel → `trade-flow-over-time.png`); `chart_treaty_status_over_time` extended to 4 panels when `objective-metrics.csv` exists (adds objective-status timeline). Four new scenarios: `trade-flow`, `trade-missed-payment`, `trade-source-lost`, `objective-port`. `harness.md` updated with all new scenarios, output CSVs, and chart descriptions. All 12 scenarios pass.
